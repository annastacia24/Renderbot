services:
  - type: web
    name: RenderBot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    envVars:
      - key: TELEGRAM_BOT_TOKEN
        value: your_telegram_bot_token_here
      - key: MT5_BRIDGE_URL
        value: http://your_windows_ip:5000